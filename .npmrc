# @formicoidea/wardley-map-renderer is published to the public npm registry,
# which is already npm's default — no scope routing, and no token, here.
#
# Publishing authenticates through the browser:
#   npm login --scope=@formicoidea --auth-type=web
# See PUBLISHING.md. Never hardcode a token in this file.
